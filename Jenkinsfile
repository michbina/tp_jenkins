pipeline {
  agent any
  stages {
    stage('stage 1') {
      steps {
        sh '''date




dpkg -l > /tmp/paquets
if test `grep -c user /etc/passwd` -ne 0
then
find / -user user > /tmp/user
fi'''
      }
    }

  }
}